{
    "name": "techub",
    "private": true,
    "scripts": {
      "dev": "turbo run dev",
      "dev:web": "turbo run dev --filter=web",
      "build": "turbo run build",
      "lint": "turbo run lint",
      "typecheck": "turbo run typecheck",
      "db:migrate": "cd services/cv-aggregator && npx prisma migrate dev",
      "db:studio": "cd services/cv-aggregator && npx prisma studio"
    },
    "devDependencies": {
      "turbo": "^2.0.0",
      "typescript": "^5.4.0",
      "@types/node": "^20.0.0"
    }
  }