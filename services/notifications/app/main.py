from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.health import router as health_router

# ?????????????????????????????????????????????
# App Init
# ?????????????????????????????????????????????
app = FastAPI(
    title="TECHub ? notifications",
    description="Push notifications via Firebase FCM, in-app bell, email via Resend. Manages preferences.",
    version="0.0.1",
    docs_url="/docs",
    redoc_url="/redoc",
)

# ?????????????????????????????????????????????
# CORS ? Allow frontend to talk to this service
# ?????????????????????????????????????????????
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "https://techub.io"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# ?????????????????????????????????????????????
# Routers ? Sprint 0: health only
# Sprint 1+ will add feature routers here
# ?????????????????????????????????????????????
app.include_router(health_router, tags=["Health"])

# ?????????????????????????????????????????????
# Future routers ? DO NOT REMOVE THESE COMMENTS
# They show exactly where Sprint 1+ code plugs in
# ?????????????????????????????????????????????

# SPRINT 2+ ROUTERS (notifications):
# from app.routers import push, email, bell, preferences
# app.include_router(push.router, prefix="/push", tags=["Push"])
# app.include_router(email.router, prefix="/email", tags=["Email"])
# app.include_router(bell.router, prefix="/bell", tags=["In-App Bell"])

