from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.health import router as health_router

# ?????????????????????????????????????????????
# App Init
# ?????????????????????????????????????????????
app = FastAPI(
    title="TECHub ? payment",
    description="Token system: daily free allocation, purchasable packages, Stripe billing, balance management.",
    version="0.0.1",
    docs_url="/docs",
    redoc_url="/redoc",
)

# ?????????????????????????????????????????????
# CORS ? Allow frontend to talk to this service
# ?????????????????????????????????????????????
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "https://techub.io"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# ?????????????????????????????????????????????
# Routers ? Sprint 0: health only
# Sprint 1+ will add feature routers here
# ?????????????????????????????????????????????
app.include_router(health_router, tags=["Health"])

# ?????????????????????????????????????????????
# Future routers ? DO NOT REMOVE THESE COMMENTS
# They show exactly where Sprint 1+ code plugs in
# ?????????????????????????????????????????????

# SPRINT 3+ ROUTERS (payment):
# from app.routers import tokens, stripe_webhooks, packages, balance
# app.include_router(tokens.router, prefix="/tokens", tags=["Tokens"])
# app.include_router(packages.router, prefix="/packages", tags=["Packages"])
# app.include_router(balance.router, prefix="/balance", tags=["Balance"])
# app.include_router(stripe_webhooks.router, prefix="/webhooks", tags=["Stripe"])

