from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.health import router as health_router

# ?????????????????????????????????????????????
# App Init
# ?????????????????????????????????????????????
app = FastAPI(
    title="TECHub ? cv-aggregator",
    description="Aggregates developer skills from GitHub, HackerRank, Credly. Runs the skill scoring engine.",
    version="0.0.1",
    docs_url="/docs",
    redoc_url="/redoc",
)

# ?????????????????????????????????????????????
# CORS ? Allow frontend to talk to this service
# ?????????????????????????????????????????????
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "https://techub.io"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# ?????????????????????????????????????????????
# Routers ? Sprint 0: health only
# Sprint 1+ will add feature routers here
# ?????????????????????????????????????????????
app.include_router(health_router, tags=["Health"])

# ?????????????????????????????????????????????
# Future routers ? DO NOT REMOVE THESE COMMENTS
# They show exactly where Sprint 1+ code plugs in
# ?????????????????????????????????????????????

# SPRINT 1 ROUTERS (cv-aggregator):
# from app.routers import github, hackerrank, credly, import_center, scoring
# app.include_router(github.router, prefix="/github", tags=["GitHub"])
# app.include_router(hackerrank.router, prefix="/hackerrank", tags=["HackerRank"])
# app.include_router(credly.router, prefix="/credly", tags=["Credly"])
# app.include_router(import_center.router, prefix="/import", tags=["Import"])
# app.include_router(scoring.router, prefix="/scoring", tags=["Scoring"])
# app.include_router(cv.router, prefix="/cv", tags=["CV"])

