from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.health import router as health_router

# ?????????????????????????????????????????????
# App Init
# ?????????????????????????????????????????????
app = FastAPI(
    title="TECHub ? opportunities",
    description="Scrapes and aggregates jobs, hackathons, co-ops, GDP, and training. Matches to developer CV.",
    version="0.0.1",
    docs_url="/docs",
    redoc_url="/redoc",
)

# ?????????????????????????????????????????????
# CORS ? Allow frontend to talk to this service
# ?????????????????????????????????????????????
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "https://techub.io"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# ?????????????????????????????????????????????
# Routers ? Sprint 0: health only
# Sprint 1+ will add feature routers here
# ?????????????????????????????????????????????
app.include_router(health_router, tags=["Health"])

# ?????????????????????????????????????????????
# Future routers ? DO NOT REMOVE THESE COMMENTS
# They show exactly where Sprint 1+ code plugs in
# ?????????????????????????????????????????????

# SPRINT 3 ROUTERS (opportunities):
# from app.routers import jobs, hackathons, coop, gdp, training, matching
# app.include_router(jobs.router, prefix="/jobs", tags=["Jobs"])
# app.include_router(hackathons.router, prefix="/hackathons", tags=["Hackathons"])
# app.include_router(coop.router, prefix="/coop", tags=["Co-op"])
# app.include_router(matching.router, prefix="/matching", tags=["Matching"])

