{
  "name": "@techub/opportunities",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --host 0.0.0.0 --port 8003 --reload",
    "test": "python -m pytest tests -v",
    "install:python": "pip install -r requirements.txt"
  }
}
