from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.health import router as health_router

# ?????????????????????????????????????????????
# App Init
# ?????????????????????????????????????????????
app = FastAPI(
    title="TECHub ? gamification",
    description="XP engine, level system, badge awards, leaderboard, daily quests, streak tracking.",
    version="0.0.1",
    docs_url="/docs",
    redoc_url="/redoc",
)

# ?????????????????????????????????????????????
# CORS ? Allow frontend to talk to this service
# ?????????????????????????????????????????????
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "https://techub.io"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# ?????????????????????????????????????????????
# Routers ? Sprint 0: health only
# Sprint 1+ will add feature routers here
# ?????????????????????????????????????????????
app.include_router(health_router, tags=["Health"])

# ?????????????????????????????????????????????
# Future routers ? DO NOT REMOVE THESE COMMENTS
# They show exactly where Sprint 1+ code plugs in
# ?????????????????????????????????????????????

# SPRINT 2 ROUTERS (gamification):
# from app.routers import xp, levels, badges, leaderboard, quests, streaks
# app.include_router(xp.router, prefix="/xp", tags=["XP"])
# app.include_router(levels.router, prefix="/levels", tags=["Levels"])
# app.include_router(badges.router, prefix="/badges", tags=["Badges"])
# app.include_router(leaderboard.router, prefix="/leaderboard", tags=["Leaderboard"])
# app.include_router(quests.router, prefix="/quests", tags=["Daily Quests"])

